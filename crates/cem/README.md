# CEM crate

This crate provides a minimal calibration engine (CEM) for neuromorphic/biomechanical energy estimation.

Run tests:

```
cargo test -p cem
```

Run CLI:

```
cargo run -p cem -- --subject 1 --session 1 --input examples/sample_dataset.json
```
