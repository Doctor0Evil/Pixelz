[package]
name = "aln_trader_pod"
version = "0.1.0"
edition = "2021"

[lib]
name = "aln_trader_pod"
path = "src/lib.rs"

[dependencies]
ndarray = "0.15"
ndarray-linalg = { version = "0.18", optional = true }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
